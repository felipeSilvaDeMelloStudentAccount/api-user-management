server:
  port: 9001
  servlet:
    context-path: /api
jwt:
  secret: ${jwt.secret:secret}
  expiration: ${jwt.expiration:3600000} # 1 hour
spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${mongodb+srv://serverconnection:XeLNDTdl8tFmC2xa@mflix-jho7e.mongodb.net/test?retryWrites=true&w=majority}
      database: ${database:dev}
